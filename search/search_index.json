{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"]},"docs":[{"location":"CONFIGURATION/","title":"Configuration","text":"<p>Projects use <code>neleus.toml</code>.</p>"},{"location":"CONFIGURATION/#default-scaffold","title":"Default Scaffold","text":"<pre><code>[project]\nname = \"my_strategy_project\"\nversion = \"0.1.0\"\n\n[hyperliquid]\ntestnet = false\n\n[market]\nsymbol = \"BTC-PERP\"\ntimeframe = \"1h\"\nlookback_bars = 200\n\n[backtest]\ninitial_capital = 10000.0\nmaker_fee_bps = 2.0\ntaker_fee_bps = 5.0\nslippage_bps = 5.0\n\n[runtime]\nmode = \"once\"\npoll_interval_seconds = 60\n\n[database]\nbackend = \"none\"\ndsn = \"\"\npool_size = 4\nbatch_size = 1000\nflush_interval_ms = 100\ntrade_monitoring = false\n</code></pre>"},{"location":"CONFIGURATION/#database-section","title":"Database Section","text":"<p>The active persistence section is <code>[database]</code>.</p> <p><code>database.backend</code> accepts these values:</p> <ul> <li><code>none</code>: disable persistence entirely</li> <li><code>postgres</code>: enable the PostgreSQL adapter</li> <li><code>timescale</code>: enable the TimescaleDB adapter</li> </ul> <p>Fields:</p> <ul> <li><code>backend</code>: persistence mode and adapter selection</li> <li><code>dsn</code>: PostgreSQL connection string</li> <li><code>pool_size</code>: connection pool size</li> <li><code>batch_size</code>: batch insert size for the Timescale path</li> <li><code>flush_interval_ms</code>: flush interval for the Timescale path</li> <li><code>trade_monitoring</code>: automatically record generated orders and fills via <code>TradeMonitor</code></li> </ul> <p>Recommended secret handling:</p> <ul> <li>keep <code>database.dsn = \"\"</code> in committed <code>neleus.toml</code></li> <li>set <code>NELEUS_DB_DSN</code> in a local <code>.env</code></li> <li>let the scaffolded <code>.gitignore</code> keep that <code>.env</code> file out of version control</li> </ul>"},{"location":"CONFIGURATION/#environment-variables","title":"Environment Variables","text":"<pre><code># Public Hyperliquid market data uses /info and does not need credentials.\nHYPERLIQUID_TESTNET=false\n\n# For future signed trading flows.\n# HYPERLIQUID_ACCOUNT_ADDRESS=0x...\n# HYPERLIQUID_SIGNER_PRIVATE_KEY=\n\nNELEUS_DB_BACKEND=postgres\nNELEUS_DB_DSN=\n</code></pre> <p>Currently supported DB-related environment overrides:</p> <ul> <li><code>NELEUS_DB_BACKEND</code></li> <li><code>NELEUS_DB_DSN</code></li> <li><code>NELEUS_DB_POOL_SIZE</code></li> </ul>"},{"location":"CONFIGURATION/#scaffold-with-database-monitoring","title":"Scaffold With Database Monitoring","text":"<pre><code>neleus new my_strategy_project \\\n  --db-backend postgres \\\n  --db-dsn postgresql://user:password@localhost:5432/neleus \\\n  --trade-monitoring\n</code></pre> <p>When you pass <code>--db-dsn</code>, Neleus writes that value into a local <code>.env</code> file and keeps <code>database.dsn</code> empty in <code>neleus.toml</code>.</p> <p>Then initialize the schema:</p> <pre><code>cd my_strategy_project\nneleus db init\n</code></pre>"},{"location":"CONFIGURATION/#runtime-monitoring-behavior","title":"Runtime Monitoring Behavior","text":"<p>When <code>database.trade_monitoring = true</code> and a DSN is configured, the runtime:</p> <ol> <li>resolves a <code>DatabaseConfig</code> from project config and environment overrides</li> <li>creates a <code>TradeMonitor</code> once at startup</li> <li>records every generated order after <code>ctx.drain_order_requests()</code></li> </ol> <p>Each generated order is stored with:</p> <ul> <li>a UUID client order ID (<code>cloid</code>)</li> <li>the project <code>testnet</code> flag</li> </ul>"},{"location":"CONFIGURATION/#python-config-api","title":"Python Config API","text":"<p>The top-level package re-exports <code>DatabaseConfig</code> and <code>get_db_config</code>.</p> <pre><code>from pathlib import Path\n\nfrom neleus import DatabaseConfig, get_db_config, load_project_config\n\nconfig = load_project_config(Path(\"neleus.toml\"))\ndb_cfg: DatabaseConfig = get_db_config(config)\n\nprint(db_cfg.backend)\nprint(db_cfg.trade_monitoring)\nprint(db_cfg.dsn)\n</code></pre>"},{"location":"CONFIGURATION/#notes","title":"Notes","text":"<ul> <li>Mainnet is the default.</li> <li>The currently implemented CLI market workflows do not require signing credentials.</li> <li><code>neleus db status</code> checks the configured database connection for the current project.</li> <li><code>neleus db init</code> creates <code>hl_orders</code> / <code>hl_fills</code> through <code>TradeMonitor</code>.</li> <li>for <code>timescale</code>, <code>neleus db init</code> also initializes the TimescaleDB market-data schema.</li> </ul>"},{"location":"INDEX/","title":"NeleusNeleus","text":"<p>Trade Hyperliquid from the terminal.</p> <p>     Neleus is a Hyperliquid-first CLI and Python toolkit powered by a Rust core.     Use it to search markets, run technical scans, stream live order books,     backtest strategies, and scaffold Python trading projects.   </p> Get Started Market Workflows Website GitHub No-project market tools <p>Search, list, analyze, scan, and monitor Hyperliquid markets directly from the CLI.</p> Rust-backed market core <p>REST and WebSocket paths are driven by the Rust Hyperliquid adapter, then exposed to Python and the CLI.</p> Python strategy workflow <p>Scaffold a project, write strategies in Python, backtest them, and run them once or as a daemon.</p> Terminal-first UX <p>Rich dashboards, ranked scans, live order book views, and concise command-driven workflows.</p>"},{"location":"INDEX/#what-you-can-do-today","title":"What You Can Do Today","text":"Workflow Command Result Search markets <code>neleus market search BTC</code> Find matching spot, default perp, or HIP-3 markets List market catalogs <code>neleus market list --scope hip3 --dex xyz</code> View market groups with counts and metadata Analyze a market <code>neleus market analyze BTC-PERP</code> Get RSI, trend, levels, volatility, and a directional read Scan setups <code>neleus market scan --scope perps</code> Rank a bounded market set by conviction-style TA score Stream live depth <code>neleus market book BTC-PERP</code> Watch a live Hyperliquid L2 order book in the terminal Scaffold a project <code>neleus new my_strategy_project</code> Generate a Python strategy project wired to the Rust core"},{"location":"INDEX/#product-scope","title":"Product Scope","text":"<p>Implemented now: - market search, list, analysis, scanning, and live order book monitoring - project scaffolding for Python strategies - backtesting - one-shot and daemon strategy runtimes - database-backed runtime order/fill monitoring through <code>TradeMonitor</code> - database schema inspection and initialization through <code>neleus db status</code> and <code>neleus db init</code></p> <p>Not implemented yet: - a dedicated <code>neleus trade</code> command separate from the current runtime/core APIs - broader live operations tooling beyond the current trade-monitoring path</p>"},{"location":"INDEX/#suggested-reading-order","title":"Suggested Reading Order","text":"<ol> <li>Start with Installation</li> <li>Run the no-project commands in Quickstart</li> <li>Learn the full market command surface in Market Workflows</li> <li>Scaffold a project in Strategy Projects</li> <li>Learn the actual strategy API in Writing Strategies</li> <li>Copy and adapt code from Strategy Examples</li> </ol>"},{"location":"projects/","title":"Strategy Projects","text":"<p>Create a project when you want to write Python strategy code on top of the Neleus Rust core.</p> <p>This page covers:</p> <ul> <li>how to scaffold a project</li> <li>what files Neleus generates</li> <li>the recommended workflow from scaffold to runtime</li> <li>where strategy-specific config files live</li> </ul>"},{"location":"projects/#scaffold-a-new-project","title":"Scaffold A New Project","text":"<p>Create a new directory:</p> <pre><code>neleus new my_strategy_project\ncd my_strategy_project\n</code></pre> <p>Initialize the current directory instead:</p> <pre><code>mkdir my_strategy_project\ncd my_strategy_project\nneleus init\n</code></pre>"},{"location":"projects/#scaffold-with-database-monitoring","title":"Scaffold With Database Monitoring","text":"<p>You can scaffold the database section directly from the CLI:</p> <pre><code>neleus new my_strategy_project \\\n  --db-backend postgres \\\n  --db-dsn postgresql://user:password@localhost:5432/neleus \\\n  --trade-monitoring\n</code></pre> <p>If you pass <code>--db-dsn</code>, Neleus stores it in a local <code>.env</code> file and keeps <code>database.dsn = \"\"</code> in <code>neleus.toml</code> so credentials are not scaffolded into a committed config file.</p> <p>Supported <code>--db-backend</code> values:</p> <ul> <li><code>none</code>: leave persistence disabled</li> <li><code>postgres</code>: enable the PostgreSQL adapter</li> <li><code>timescale</code>: enable the TimescaleDB adapter</li> </ul>"},{"location":"projects/#what-gets-generated","title":"What Gets Generated","text":"<p>The scaffolded project looks like this:</p> <pre><code>my_strategy_project/\n\u251c\u2500\u2500 .gitignore\n\u251c\u2500\u2500 .env                 # only created when --db-dsn is supplied\n\u251c\u2500\u2500 neleus.toml\n\u251c\u2500\u2500 .env.example\n\u251c\u2500\u2500 main.py\n\u2514\u2500\u2500 strategies/\n    \u251c\u2500\u2500 __init__.py\n    \u2514\u2500\u2500 momentum.py\n</code></pre>"},{"location":"projects/#neleustoml","title":"<code>neleus.toml</code>","text":"<p>This is the project config. It holds:</p> <ul> <li>project metadata</li> <li>Hyperliquid network selection</li> <li>default market symbol and timeframe</li> <li>backtest parameters</li> <li>runtime mode and polling interval</li> <li>database adapter settings</li> <li>trade-monitoring settings</li> </ul> <p>Scaffolded example:</p> <pre><code>[project]\nname = \"my_strategy_project\"\nversion = \"0.1.0\"\n\n[hyperliquid]\ntestnet = false\n\n[market]\nsymbol = \"BTC-PERP\"\ntimeframe = \"1h\"\nlookback_bars = 200\n\n[backtest]\ninitial_capital = 10000.0\nmaker_fee_bps = 2.0\ntaker_fee_bps = 5.0\nslippage_bps = 5.0\n\n[runtime]\nmode = \"once\"\npoll_interval_seconds = 60\n\n[database]\nbackend = \"none\"\ndsn = \"\"\npool_size = 4\nbatch_size = 1000\nflush_interval_ms = 100\ntrade_monitoring = false\n</code></pre>"},{"location":"projects/#envexample","title":"<code>.env.example</code>","text":"<p>This is where you document local overrides such as:</p> <ul> <li><code>HYPERLIQUID_TESTNET</code></li> <li><code>NELEUS_DB_BACKEND</code></li> <li><code>NELEUS_DB_DSN</code></li> </ul> <p>If you scaffold with <code>--db-dsn</code>, Neleus also creates a local <code>.env</code> file and adds <code>.env</code> to the project <code>.gitignore</code>.</p>"},{"location":"projects/#mainpy","title":"<code>main.py</code>","text":"<p>The scaffold includes a simple Python entrypoint:</p> <pre><code>from pathlib import Path\n\nfrom neleus.runtime import run_project_once\n\n\nif __name__ == \"__main__\":\n    result = run_project_once(Path(__file__).parent)\n    print(result.to_dict())\n</code></pre> <p>You can run that directly with <code>python main.py</code>, but the CLI is the primary workflow:</p> <pre><code>neleus run --mode once\n</code></pre>"},{"location":"projects/#strategiesmomentumpy","title":"<code>strategies/momentum.py</code>","text":"<p>This is your first strategy file. It shows the basic pattern:</p> <ul> <li>subclass <code>Strategy</code></li> <li>keep your own state on <code>self</code></li> <li>react to <code>on_bar</code></li> <li>emit orders through <code>StrategyContext</code></li> </ul>"},{"location":"projects/#recommended-project-workflow","title":"Recommended Project Workflow","text":"<ol> <li>Scaffold the project with <code>neleus new</code>.</li> <li>If you enabled a database backend, initialize the schema:</li> </ol> <pre><code>neleus db init\n</code></pre> <ol> <li>Inspect the generated strategy:</li> </ol> <pre><code>neleus strategy list\nneleus strategy show momentum\n</code></pre> <ol> <li>Edit <code>neleus.toml</code> to choose the market and timeframe you want.</li> <li>Edit the strategy file under <code>strategies/</code>.</li> <li>Backtest the strategy:</li> </ol> <pre><code>neleus backtest --strategy momentum\n</code></pre> <ol> <li>Run the strategy once against recent market data:</li> </ol> <pre><code>neleus run --mode once --strategy momentum\n</code></pre> <ol> <li>Run it continuously:</li> </ol> <pre><code>neleus run --mode daemon --strategy momentum\n</code></pre>"},{"location":"projects/#add-more-strategies","title":"Add More Strategies","text":"<p>Create a new strategy file:</p> <pre><code>neleus strategy new breakout\n</code></pre> <p>That creates:</p> <pre><code>strategies/breakout.py\n</code></pre> <p>List discovered strategies:</p> <pre><code>neleus strategy list\n</code></pre> <p>Neleus discovers strategies from <code>strategies/*.py</code>, skipping files that start with <code>_</code>.</p>"},{"location":"projects/#strategy-specific-config-files","title":"Strategy-Specific Config Files","text":"<p>You can add an optional config file for a strategy under <code>configs/</code>.</p> <p>Example:</p> <pre><code>my_strategy_project/\n\u251c\u2500\u2500 configs/\n\u2502   \u2514\u2500\u2500 momentum.yaml\n\u2514\u2500\u2500 strategies/\n    \u2514\u2500\u2500 momentum.py\n</code></pre> <p>Example config:</p> <pre><code>strategy:\n  enabled: true\n  class: MomentumStrategy\n  parameters:\n    lookback: 30\n</code></pre> <p>Important:</p> <ul> <li><code>neleus backtest</code> reads <code>configs/&lt;strategy&gt;.yaml</code> and passes <code>strategy.parameters</code> into the strategy constructor.</li> <li>the current <code>neleus run</code> project runtime instantiates the strategy with no constructor arguments</li> </ul> <p>That means constructor parameters should have defaults if you want the same strategy to work in both backtests and the runtime.</p>"},{"location":"projects/#useful-project-commands","title":"Useful Project Commands","text":"<pre><code>neleus strategy list\nneleus strategy new breakout\nneleus strategy show breakout\nneleus db status\nneleus db init\nneleus info\nneleus backtest --strategy breakout\nneleus run --mode once --strategy breakout\nneleus run --mode daemon --strategy breakout\n</code></pre>"},{"location":"projects/#database-backed-trade-monitoring","title":"Database-Backed Trade Monitoring","text":"<p>When a project has:</p> <ul> <li><code>database.backend = \"postgres\"</code> or <code>database.backend = \"timescale\"</code></li> <li>a configured <code>database.dsn</code></li> <li><code>database.trade_monitoring = true</code></li> </ul> <p>the runtime will automatically:</p> <ol> <li>create a <code>TradeMonitor</code> once at startup</li> <li>run your strategy normally</li> <li>drain generated order requests from each <code>StrategyContext</code></li> <li>record each generated order to the configured database</li> </ol> <p>Each generated order is tagged with:</p> <ul> <li>a UUID client order ID (<code>cloid</code>)</li> <li>the project's <code>testnet</code> flag</li> </ul> <p>This applies to:</p> <ul> <li><code>neleus run --mode once</code></li> <li><code>neleus run --mode daemon</code></li> <li><code>run_project_once(...)</code></li> <li><code>run_project_daemon(...)</code></li> </ul> <p>The strategy code does not need any explicit DB calls for this flow.</p>"},{"location":"projects/#what-the-current-runtime-actually-does","title":"What The Current Runtime Actually Does","text":"<p>For project strategies, the current runtime flow is:</p> <ol> <li>load the strategy class from <code>strategies/&lt;name&gt;.py</code></li> <li>instantiate it</li> <li>call <code>on_start(...)</code></li> <li>fetch recent Hyperliquid candles</li> <li>convert each candle into a <code>Bar</code></li> <li>call <code>on_bar(...)</code> and then <code>on_data(...)</code></li> <li>drain any generated order requests</li> <li>if trade monitoring is enabled, record the generated orders through <code>TradeMonitor</code></li> <li>call <code>on_stop(...)</code></li> </ol> <p>That makes the current <code>neleus run</code> flow bar-driven and simple to reason about. It is useful for strategy development, but it is not yet a full live execution engine.</p> <p>For the actual strategy-writing API, continue to Writing Strategies.</p>"},{"location":"strategy-examples/","title":"Strategy Examples","text":"<p>This page gives copy-pasteable examples for project strategies.</p>"},{"location":"strategy-examples/#example-1-scaffolded-momentum-strategy","title":"Example 1: Scaffolded Momentum Strategy","text":"<p>This is the pattern generated by <code>neleus new</code>:</p> <pre><code>from neleus import OrderSide, Strategy, StrategyContext, Bar\n\n\nclass MomentumStrategy(Strategy):\n    def __init__(self, lookback: int = 20):\n        super().__init__(\"momentum\")\n        self.lookback = lookback\n        self.prices: list[float] = []\n\n    def on_bar(self, ctx: StrategyContext, bar: Bar) -&gt; None:\n        self.prices.append(float(bar.close))\n        if len(self.prices) &lt; self.lookback:\n            return\n\n        window = self.prices[-self.lookback:]\n        baseline = sum(window[:-1]) / max(len(window) - 1, 1)\n        if bar.close &gt; baseline * 1.01:\n            ctx.market_order(bar.instrument_id, OrderSide.Buy, 0.01)\n</code></pre> <p>Use it with:</p> <pre><code>neleus backtest --strategy momentum\nneleus run --mode once --strategy momentum\n</code></pre>"},{"location":"strategy-examples/#example-2-long-only-pullback-strategy","title":"Example 2: Long-Only Pullback Strategy","text":"<pre><code>from neleus import Bar, OrderSide, Strategy, StrategyContext\n\n\nclass PullbackLongStrategy(Strategy):\n    def __init__(self, lookback: int = 25, pullback_pct: float = 0.015):\n        super().__init__(\"pullback_long\")\n        self.lookback = lookback\n        self.pullback_pct = pullback_pct\n        self.prices: list[float] = []\n        self.in_position = False\n\n    def on_bar(self, ctx: StrategyContext, bar: Bar) -&gt; None:\n        close = float(bar.close)\n        self.prices.append(close)\n\n        if len(self.prices) &lt; self.lookback:\n            return\n\n        moving_average = sum(self.prices[-self.lookback:]) / self.lookback\n        drawdown_from_average = (close / moving_average) - 1.0\n\n        if not self.in_position and drawdown_from_average &lt;= -self.pullback_pct:\n            ctx.market_order(bar.instrument_id, OrderSide.Buy, 0.01)\n            self.in_position = True\n            return\n\n        if self.in_position and close &gt;= moving_average:\n            ctx.market_order(bar.instrument_id, OrderSide.Sell, 0.01, reduce_only=True)\n            self.in_position = False\n</code></pre>"},{"location":"strategy-examples/#example-3-parameterized-strategy-with-backtest-config","title":"Example 3: Parameterized Strategy With Backtest Config","text":"<p>Strategy file:</p> <pre><code>from neleus import Bar, OrderSide, Strategy, StrategyContext\n\n\nclass SlowMomentumStrategy(Strategy):\n    def __init__(self, lookback: int = 40, trigger_pct: float = 0.01):\n        super().__init__(\"slow_momentum\")\n        self.lookback = lookback\n        self.trigger_pct = trigger_pct\n        self.prices: list[float] = []\n\n    def on_bar(self, ctx: StrategyContext, bar: Bar) -&gt; None:\n        close = float(bar.close)\n        self.prices.append(close)\n        if len(self.prices) &lt; self.lookback:\n            return\n\n        average = sum(self.prices[-self.lookback:]) / self.lookback\n        if close &gt;= average * (1.0 + self.trigger_pct):\n            ctx.market_order(bar.instrument_id, OrderSide.Buy, 0.01)\n</code></pre> <p>Config file at <code>configs/slow_momentum.yaml</code>:</p> <pre><code>strategy:\n  enabled: true\n  class: SlowMomentumStrategy\n  parameters:\n    lookback: 50\n    trigger_pct: 0.008\n</code></pre> <p>Run:</p> <pre><code>neleus backtest --strategy slow_momentum\n</code></pre>"},{"location":"strategy-examples/#example-4-add-a-new-strategy-with-the-cli","title":"Example 4: Add A New Strategy With The CLI","text":"<pre><code>neleus strategy new breakout\n</code></pre> <p>Then edit <code>strategies/breakout.py</code>, inspect it:</p> <pre><code>neleus strategy show breakout\n</code></pre> <p>And run it:</p> <pre><code>neleus backtest --strategy breakout\nneleus run --mode once --strategy breakout\n</code></pre>"},{"location":"strategy-examples/#suggested-development-loop","title":"Suggested Development Loop","text":"<ol> <li>Start from the scaffolded <code>momentum.py</code></li> <li>Rename or create a new strategy with <code>neleus strategy new</code></li> <li>Keep constructor parameters optional</li> <li>Backtest repeatedly</li> <li>Run once against recent data</li> <li>Move to daemon mode only after the bar-driven logic looks correct</li> </ol>"},{"location":"strategy-writing/","title":"Writing Strategies","text":"<p>This page shows how to write strategies that work with the current Neleus project workflow.</p>"},{"location":"strategy-writing/#the-core-pattern","title":"The Core Pattern","text":"<p>A Neleus strategy is a Python class that subclasses <code>Strategy</code>.</p> <p>Minimal example:</p> <pre><code>from neleus import Bar, OrderSide, Strategy, StrategyContext\n\n\nclass SimpleMomentumStrategy(Strategy):\n    def __init__(self, lookback: int = 20):\n        super().__init__(\"simple_momentum\")\n        self.lookback = lookback\n        self.prices: list[float] = []\n\n    def on_bar(self, ctx: StrategyContext, bar: Bar) -&gt; None:\n        self.prices.append(float(bar.close))\n        if len(self.prices) &lt; self.lookback:\n            return\n\n        average_price = sum(self.prices[-self.lookback:]) / self.lookback\n        if bar.close &gt; average_price * 1.01:\n            ctx.market_order(bar.instrument_id, OrderSide.Buy, 0.01)\n</code></pre>"},{"location":"strategy-writing/#strategy-lifecycle","title":"Strategy Lifecycle","text":"<p>The base class lives in the Python package and exposes these main callbacks:</p> <ul> <li><code>on_start(ctx)</code></li> <li><code>on_stop(ctx)</code></li> <li><code>on_data(ctx, data)</code></li> <li><code>on_bar(ctx, bar)</code></li> <li><code>on_trade(ctx, trade)</code></li> <li><code>on_quote(ctx, quote)</code></li> <li><code>on_book(ctx, book)</code></li> </ul> <p>For the current project runtime, the important ones are:</p> <ul> <li><code>on_start</code></li> <li><code>on_bar</code></li> <li><code>on_data</code></li> <li><code>on_stop</code></li> </ul>"},{"location":"strategy-writing/#how-neleus-run-calls-your-strategy","title":"How <code>neleus run</code> Calls Your Strategy","text":"<p>The current project runtime is bar-driven:</p> <ol> <li>it loads a <code>Strategy</code> subclass from <code>strategies/&lt;name&gt;.py</code></li> <li>it instantiates the class with no constructor arguments</li> <li>it calls <code>on_start(...)</code></li> <li>it fetches candles from Hyperliquid</li> <li>it converts each candle into a <code>Bar</code></li> <li>it calls <code>on_bar(...)</code></li> <li>it also calls <code>on_data(...)</code> with the same <code>Bar</code></li> <li>it drains order requests from the context</li> <li>it calls <code>on_stop(...)</code></li> </ol> <p>This matters for strategy authors:</p> <ul> <li>keep persistent state on <code>self</code>, not in <code>StrategyContext</code></li> <li>give constructor parameters default values if you want <code>neleus run</code> to work</li> <li>treat the current runtime as a signal/order-request generator over bars</li> <li>when project trade monitoring is enabled, generated orders are recorded automatically after each <code>ctx.drain_order_requests()</code> call</li> </ul>"},{"location":"strategy-writing/#where-strategy-state-should-live","title":"Where Strategy State Should Live","text":"<p>Good:</p> <pre><code>self.prices.append(float(bar.close))\nself.in_position = True\nself.last_signal_ts = bar.timestamp_ns\n</code></pre> <p>Not good for the current project runtime:</p> <ul> <li>assuming <code>StrategyContext</code> is persistent across bars</li> <li>assuming <code>ctx.get_position(...)</code> is a reliable live portfolio source in <code>neleus run</code></li> </ul> <p>The project runtime creates a fresh <code>StrategyContext</code> for each bar. Your strategy instance persists; the context does not.</p>"},{"location":"strategy-writing/#order-apis-you-can-use","title":"Order APIs You Can Use","text":"<p>The current order methods exposed through <code>StrategyContext</code> are:</p>"},{"location":"strategy-writing/#market-order","title":"Market order","text":"<pre><code>ctx.market_order(bar.instrument_id, OrderSide.Buy, 0.01)\nctx.market_order(bar.instrument_id, OrderSide.Sell, 0.01, reduce_only=True)\n</code></pre>"},{"location":"strategy-writing/#limit-order","title":"Limit order","text":"<pre><code>from neleus import TimeInForce\n\nctx.limit_order(\n    bar.instrument_id,\n    OrderSide.Buy,\n    price=float(bar.close) * 0.995,\n    quantity=0.01,\n    time_in_force=TimeInForce.GTC,\n)\n</code></pre>"},{"location":"strategy-writing/#cancel-order","title":"Cancel order","text":"<pre><code>ctx.cancel_order(order_id)\n</code></pre>"},{"location":"strategy-writing/#subscribe-methods","title":"Subscribe methods","text":"<p>These methods exist on <code>StrategyContext</code>:</p> <ul> <li><code>subscribe_trades(...)</code></li> <li><code>subscribe_quotes(...)</code></li> <li><code>subscribe_book(...)</code></li> </ul> <p>But the current project runtime path is bar-driven and does not yet feed <code>on_trade</code>, <code>on_quote</code>, or <code>on_book</code> for <code>neleus run</code>.</p>"},{"location":"strategy-writing/#what-happens-to-generated-orders","title":"What Happens To Generated Orders","text":"<p>When your strategy calls:</p> <pre><code>ctx.market_order(...)\nctx.limit_order(...)\n</code></pre> <p>the runtime drains those order requests after each bar.</p> <p>If project trade monitoring is enabled, the runtime also:</p> <ul> <li>creates a <code>TradeMonitor</code> once at startup</li> <li>assigns a UUID <code>cloid</code> to each generated order</li> <li>records the order into the configured database</li> <li>tags the record with the runtime <code>testnet</code> flag</li> </ul> <p>That means strategy authors do not need explicit DB calls just to persist generated orders.</p>"},{"location":"strategy-writing/#use-barinstrument_id","title":"Use <code>bar.instrument_id</code>","text":"<p>The simplest pattern is to reuse the instrument from the incoming bar:</p> <pre><code>ctx.market_order(bar.instrument_id, OrderSide.Buy, 0.01)\n</code></pre> <p>That avoids constructing instrument IDs manually.</p>"},{"location":"strategy-writing/#example-mean-reversion-strategy","title":"Example: Mean Reversion Strategy","text":"<p>This example uses a simple rolling z-score style read from recent closes:</p> <pre><code>from neleus import Bar, OrderSide, Strategy, StrategyContext\n\n\nclass MeanReversionStrategy(Strategy):\n    def __init__(self, lookback: int = 20, entry_threshold: float = 0.02):\n        super().__init__(\"mean_reversion\")\n        self.lookback = lookback\n        self.entry_threshold = entry_threshold\n        self.prices: list[float] = []\n        self.in_position = False\n\n    def on_bar(self, ctx: StrategyContext, bar: Bar) -&gt; None:\n        close = float(bar.close)\n        self.prices.append(close)\n\n        if len(self.prices) &lt; self.lookback:\n            return\n\n        window = self.prices[-self.lookback:]\n        average = sum(window) / len(window)\n        deviation = (close / average) - 1.0\n\n        if not self.in_position and deviation &lt;= -self.entry_threshold:\n            ctx.market_order(bar.instrument_id, OrderSide.Buy, 0.01)\n            self.in_position = True\n            return\n\n        if self.in_position and close &gt;= average:\n            ctx.market_order(bar.instrument_id, OrderSide.Sell, 0.01, reduce_only=True)\n            self.in_position = False\n</code></pre>"},{"location":"strategy-writing/#example-breakout-strategy-with-limit-orders","title":"Example: Breakout Strategy With Limit Orders","text":"<pre><code>from neleus import Bar, OrderSide, Strategy, StrategyContext, TimeInForce\n\n\nclass BreakoutStrategy(Strategy):\n    def __init__(self, lookback: int = 30):\n        super().__init__(\"breakout\")\n        self.lookback = lookback\n        self.highs: list[float] = []\n        self.lows: list[float] = []\n\n    def on_bar(self, ctx: StrategyContext, bar: Bar) -&gt; None:\n        self.highs.append(float(bar.high))\n        self.lows.append(float(bar.low))\n\n        if len(self.highs) &lt; self.lookback:\n            return\n\n        breakout_level = max(self.highs[-self.lookback:-1])\n        breakdown_level = min(self.lows[-self.lookback:-1])\n\n        if float(bar.close) &gt; breakout_level:\n            ctx.limit_order(\n                bar.instrument_id,\n                OrderSide.Buy,\n                price=float(bar.close),\n                quantity=0.01,\n                time_in_force=TimeInForce.GTC,\n            )\n        elif float(bar.close) &lt; breakdown_level:\n            ctx.limit_order(\n                bar.instrument_id,\n                OrderSide.Sell,\n                price=float(bar.close),\n                quantity=0.01,\n                time_in_force=TimeInForce.GTC,\n            )\n</code></pre>"},{"location":"strategy-writing/#constructor-defaults-matter","title":"Constructor Defaults Matter","text":"<p>The current project runtime calls your strategy like this:</p> <pre><code>strategy = strategy_class()\n</code></pre> <p>So this is safe:</p> <pre><code>def __init__(self, lookback: int = 20, threshold: float = 0.02):\n    ...\n</code></pre> <p>This will break <code>neleus run</code>:</p> <pre><code>def __init__(self, lookback: int, threshold: float):\n    ...\n</code></pre> <p>If you want custom parameters in backtests, combine defaults with a strategy config file.</p>"},{"location":"strategy-writing/#strategy-config-example","title":"Strategy Config Example","text":"<p>Create <code>configs/mean_reversion.yaml</code>:</p> <pre><code>strategy:\n  enabled: true\n  class: MeanReversionStrategy\n  parameters:\n    lookback: 30\n    entry_threshold: 0.015\n</code></pre> <p><code>neleus backtest</code> can use those parameters when it instantiates the strategy.</p>"},{"location":"strategy-writing/#practical-tips","title":"Practical Tips","text":"<ul> <li>start with one symbol and one timeframe</li> <li>keep all persistent state on the strategy instance</li> <li>use <code>bar.instrument_id</code> when placing orders</li> <li>prefer constructors with defaults</li> <li>backtest first, then run once, then run daemon mode</li> <li>use <code>neleus strategy show &lt;name&gt;</code> to quickly inspect the active source</li> </ul> <p>For full code examples, continue to Strategy Examples.</p>"},{"location":"troubleshooting/","title":"Troubleshooting","text":""},{"location":"troubleshooting/#rust-extension-missing","title":"Rust Extension Missing","text":"<p>Symptoms:</p> <ul> <li>import errors mentioning <code>neleus_core</code></li> <li>the CLI fails immediately on startup</li> </ul> <p>Fix:</p> <pre><code>pip install maturin\nmaturin develop --release -m crates/pybridge/Cargo.toml\npip install -e python/\n</code></pre>"},{"location":"troubleshooting/#missing-python-dependencies","title":"Missing Python Dependencies","text":"<p>If you run the package without installing the Python dependencies, imports such as <code>yaml</code> can fail.</p> <p>Install the package dependencies:</p> <pre><code>pip install -e python/\n</code></pre>"},{"location":"troubleshooting/#hyperliquid-testnet-confusion","title":"Hyperliquid Testnet Confusion","text":"<p>The CLI defaults to Hyperliquid mainnet.</p> <p>Use <code>--testnet</code> only when you explicitly want the testnet market surface.</p>"},{"location":"troubleshooting/#scanner-is-not-exchange-wide","title":"Scanner Is Not Exchange-Wide","text":"<p><code>market scan</code> is intentionally bounded with <code>--max-markets</code> so the CLI remains fast and useful.</p> <p>If you need a narrower focus:</p> <ul> <li>use <code>--search</code></li> <li>use <code>--symbols</code></li> <li>use <code>--scope</code> and <code>--dex</code></li> </ul>"},{"location":"cli/market/","title":"Market Workflows","text":"<p>The market command group is the main no-project surface of Neleus.</p>"},{"location":"cli/market/#search-markets","title":"Search Markets","text":"<p>Use search when you know part of a market name but not the exact symbol.</p> <pre><code>neleus market search BTC\nneleus market search TSLA --scope hip3\nneleus market search PURR --scope spot\n</code></pre> <p>Useful flags:</p> <ul> <li><code>--scope perps|hip3|all-perps|spot</code></li> <li><code>--dex xyz</code></li> <li><code>--output json</code></li> </ul>"},{"location":"cli/market/#list-market-catalogs","title":"List Market Catalogs","text":"<p>Use list when you want counts, grouping, and a full market inventory.</p> <pre><code>neleus market list --scope perps\nneleus market list --scope hip3\nneleus market list --scope hip3 --dex xyz\nneleus market list --scope all-perps\nneleus market list --scope spot\n</code></pre>"},{"location":"cli/market/#analyze-a-single-market","title":"Analyze A Single Market","text":"<pre><code>neleus market analyze BTC-PERP\nneleus market analyze ETH-PERP --timeframe 15m --lookback-bars 300\nneleus market analyze BTC-PERP --output json\n</code></pre> <p>The analysis view includes:</p> <ul> <li>last price</li> <li>percentage change over the analyzed window</li> <li>volatility estimate</li> <li>RSI</li> <li>SMA and EMA structure</li> <li>Bollinger bands</li> <li>support and resistance</li> <li>directional bias and a short trading read</li> </ul>"},{"location":"cli/market/#run-a-ta-scan","title":"Run A TA Scan","text":"<p>Use the scanner when you want ranked terminal output instead of one-off analysis.</p> <pre><code>neleus market scan --scope perps\nneleus market scan --scope hip3 --dex xyz --search TSLA\nneleus market scan --symbols BTC-PERP,ETH-PERP,SOL-PERP --sort score\n</code></pre> <p>Important flags:</p> <ul> <li><code>--scope perps|hip3|all-perps|spot</code></li> <li><code>--symbols BTC-PERP,ETH-PERP,...</code></li> <li><code>--search &lt;text&gt;</code></li> <li><code>--max-markets &lt;n&gt;</code></li> <li><code>--limit &lt;n&gt;</code></li> <li><code>--sort score|change|volatility|rsi</code></li> <li><code>--timeframe 1m|5m|15m|1h|4h|1d</code></li> </ul> <p>The scanner is intentionally bounded. It is designed for fast terminal triage, not exhaustive exchange-wide screening in one command.</p>"},{"location":"cli/market/#watch-a-live-order-book","title":"Watch A Live Order Book","text":"<pre><code>neleus market book BTC-PERP\nneleus market book HYPE-PERP --depth 15\n</code></pre> <p>The live view is backed by the Rust Hyperliquid WebSocket path and shows:</p> <ul> <li>live bids and asks</li> <li>best bid and ask</li> <li>spread and spread in basis points</li> <li>total displayed depth</li> <li>order book imbalance</li> </ul> <p>Press <code>Ctrl+C</code> to stop streaming.</p>"},{"location":"cli/market/#hyperliquid-api-model","title":"Hyperliquid API Model","text":"<p>Current Neleus market workflows use Hyperliquid's public market-data surface:</p> <ul> <li><code>POST /info</code> for market metadata and candles</li> <li>WebSocket subscriptions for live market streams</li> </ul> <p>These flows do not require API keys.</p>"},{"location":"cli/overview/","title":"CLI Overview","text":"<p>The CLI is split into two layers:</p> <ul> <li>Global market commands that work without a project</li> <li>Project commands that operate on a directory containing <code>neleus.toml</code></li> </ul>"},{"location":"cli/overview/#global-commands","title":"Global Commands","text":"Command Purpose <code>neleus about</code> Show branding, links, and a short command guide <code>neleus market search &lt;query&gt;</code> Find markets by name across supported scopes <code>neleus market list</code> Show market catalogs for perps, HIP-3, all-perps, or spot <code>neleus market analyze &lt;symbol&gt;</code> Run single-market technical analysis <code>neleus market scan</code> Rank a bounded set of markets by scan score <code>neleus market book &lt;symbol&gt;</code> Stream a live L2 order book in the terminal"},{"location":"cli/overview/#project-commands","title":"Project Commands","text":"Command Purpose <code>neleus new &lt;name&gt;</code> Scaffold a new project, optionally with DB monitoring enabled <code>neleus init</code> Initialize the current directory as a project <code>neleus backtest</code> Run strategy backtests <code>neleus run --mode once|daemon</code> Execute the runtime once or continuously <code>neleus strategy list|new|show</code> Manage strategy source files <code>neleus db status</code> Show the configured backend, DSN, pool size, and monitoring state <code>neleus db init</code> Initialize the <code>hl_orders</code> / <code>hl_fills</code> schema and Timescale tables when applicable <code>neleus info</code> Show project configuration and strategy discovery"},{"location":"cli/overview/#current-limits","title":"Current Limits","text":"<ul> <li>A dedicated <code>neleus trade</code> command is not exposed yet.</li> <li>Database support currently focuses on runtime order/fill monitoring through <code>TradeMonitor</code>.</li> <li>The TA scanner intentionally limits how many markets it scores per run so terminal use stays responsive.</li> </ul>"},{"location":"getting-started/installation/","title":"Installation","text":"<p>Neleus currently requires the Rust extension. The CLI and Python package sit on top of the <code>neleus_core</code> module built from <code>crates/pybridge</code>.</p>"},{"location":"getting-started/installation/#requirements","title":"Requirements","text":"<ul> <li>Python 3.10+</li> <li>Rust toolchain</li> <li><code>maturin</code></li> </ul>"},{"location":"getting-started/installation/#install-from-source","title":"Install From Source","text":"<pre><code>python3 -m venv .venv\nsource .venv/bin/activate\n\npip install maturin\nmaturin develop --release -m crates/pybridge/Cargo.toml\npip install -e python/\n</code></pre>"},{"location":"getting-started/installation/#verify-the-install","title":"Verify The Install","text":"<pre><code>neleus about\nneleus market search BTC\n</code></pre> <p>If the Rust extension is missing, Neleus will fail early on import. Build the pybridge first, then reinstall the Python package.</p>"},{"location":"getting-started/installation/#documentation-site","title":"Documentation Site","text":"<p>This repository ships a Zensical docs site under <code>docs/</code>.</p> <p>Run it locally:</p> <pre><code>pip install -r docs/requirements.txt\nzensical serve\n</code></pre> <p>Build static output:</p> <pre><code>zensical build\n</code></pre>"},{"location":"getting-started/quickstart/","title":"Quickstart","text":"<p>This page focuses on workflows that work before you create a project.</p>"},{"location":"getting-started/quickstart/#no-project-commands","title":"No-Project Commands","text":"<pre><code>neleus about\nneleus market search BTC\nneleus market list --scope perps\nneleus market analyze HYPE-PERP\nneleus market scan --scope perps\nneleus market book BTC-PERP\n</code></pre>"},{"location":"getting-started/quickstart/#mainnet-by-default","title":"Mainnet By Default","text":"<p>The market-facing commands use Hyperliquid mainnet by default.</p> <p>Use testnet only when you need it:</p> <pre><code>neleus market search BTC --testnet\n</code></pre>"},{"location":"getting-started/quickstart/#create-a-strategy-project","title":"Create A Strategy Project","text":"<pre><code>neleus new my_strategy_project\ncd my_strategy_project\n</code></pre> <p>With PostgreSQL-backed trade monitoring enabled from the start:</p> <pre><code>neleus new my_strategy_project \\\n  --db-backend postgres \\\n  --db-dsn postgresql://user:password@localhost:5432/neleus \\\n  --trade-monitoring\n\ncd my_strategy_project\nneleus db init\n</code></pre> <p>The scaffold writes the DSN into a local <code>.env</code> file and leaves <code>database.dsn = \"\"</code> in committed <code>neleus.toml</code>, so credentials do not need to live in source control.</p> <p>Generated layout:</p> <pre><code>my_strategy_project/\n\u251c\u2500\u2500 .gitignore\n\u251c\u2500\u2500 .env                 # only created when --db-dsn is supplied\n\u251c\u2500\u2500 neleus.toml\n\u251c\u2500\u2500 .env.example\n\u251c\u2500\u2500 main.py\n\u2514\u2500\u2500 strategies/\n    \u251c\u2500\u2500 __init__.py\n    \u2514\u2500\u2500 momentum.py\n</code></pre>"},{"location":"getting-started/quickstart/#run-a-backtest","title":"Run A Backtest","text":"<pre><code>neleus backtest --strategy momentum\n</code></pre>"},{"location":"getting-started/quickstart/#run-the-strategy-runtime","title":"Run The Strategy Runtime","text":"<p>One-shot:</p> <pre><code>neleus run --mode once --strategy momentum\n</code></pre> <p>Daemon:</p> <pre><code>neleus run --mode daemon --strategy momentum\n</code></pre>"},{"location":"getting-started/quickstart/#database-backed-trade-monitoring","title":"Database-Backed Trade Monitoring","text":"<p>When your project has:</p> <ul> <li><code>database.backend = \"postgres\"</code> or <code>database.backend = \"timescale\"</code></li> <li>a configured DSN</li> <li><code>database.trade_monitoring = true</code></li> </ul> <p>the project runtime will automatically connect to the database and record every generated order from:</p> <ul> <li><code>neleus run --mode once</code></li> <li><code>neleus run --mode daemon</code></li> <li><code>run_project_once(...)</code></li> <li><code>run_project_daemon(...)</code></li> </ul>"}]}