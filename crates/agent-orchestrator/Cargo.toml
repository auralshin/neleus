[package]
name = "neleus-agent-orchestrator"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Trading agent orchestrator - CI/CD for always-on trading bots"

[dependencies]
# Workspace dependencies
neleus-core-types = { path = "../core-types" }
neleus-core-bus = { path = "../core-bus" }
neleus-core-engine = { path = "../core-engine" }
neleus-monitoring = { path = "../monitoring" }

# Serialization
serde.workspace = true
serde_json.workspace = true

# Async runtime
tokio.workspace = true
futures.workspace = true
async-trait.workspace = true

# HTTP/API
axum.workspace = true
reqwest.workspace = true

# Utilities
thiserror.workspace = true
anyhow.workspace = true
tracing.workspace = true
chrono.workspace = true
uuid.workspace = true
parking_lot.workspace = true

# Agent management
dashmap = "5.5"
cron = "0.12"

[dev-dependencies]
tokio-test = "0.4"
